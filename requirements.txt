# ─────────────────────────────────────────────────────────────────────────────
# GPS v2: requirements.txt (backwards-compatible shim)
# ─────────────────────────────────────────────────────────────────────────────
# All project dependencies are now managed via pyproject.toml.
# For a full development install, run:
#   pip install -e ".[dev]"
#
# This file is retained for backwards-compatibility with CI jobs that use
#   pip install -r requirements.txt
# and for MkDocs deployment workflows.
# ─────────────────────────────────────────────────────────────────────────────

# Documentation (MkDocs + Material theme)
mkdocs>=1.6.1
mkdocs-material>=9.5.27
mkdocs-git-revision-date-localized-plugin>=1.2.6
mike>=2.1.1

# Core GPS runtime (also declared in pyproject.toml)
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
jinja2>=3.1.4
click>=8.1.7
pyyaml>=6.0.1
rich>=13.7.0
